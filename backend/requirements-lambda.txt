# Lambda-specific requirements
# Lightweight dependencies for Lambda container

# Core dependencies
sentence-transformers>=3.0.0
torch>=2.0.0,<2.5.0
transformers>=4.30.0
numpy>=1.24.0,<2.0.0

# Optimize for CPU-only (smaller size, Lambda doesn't have GPU)
# Note: torch wheel will automatically use CPU version in Lambda container

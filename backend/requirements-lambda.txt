# Lambda-specific requirements
# Using specific versions with pre-built wheels to avoid compilation issues

# Core dependencies - install numpy first
numpy==1.26.4

# Scipy must be compatible with numpy 1.26.4 and have pre-built wheels
scipy==1.13.1

# ML dependencies
sentence-transformers==3.0.1
torch==2.8.0
transformers==4.53.0
scikit-learn==1.5.2

